'use strict';
window.$ = window.jQuery = require('jquery');

require('./main');

// angular.module('loghouseApp', [
// 	'ngRoute',
// ])
//
// .config(['$routeProvider', function($routeProvider) {
// 	$routeProvider.otherwise({
// 		redirectTo: '/'
// 	});
// }]);
